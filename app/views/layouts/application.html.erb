<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Mybpm" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Mybpm" %>">
    <%= stylesheet_link_tag "bootstrap", "sb-admin", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="wrapper">
      <header>
        <%= render 'layouts/navigation' %>
      </header>
      <%= render 'layouts/side_navbar' %>
      <div role="main" class="container">
          <%= render 'layouts/messages' %>
          <%= yield %>
      </div>
    </div>
  </body>
</html>
